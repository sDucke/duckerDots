## Script básico backup
El script en bash se encarga de clonar todos los archivos de configuración en la distro linux.
De esta forma usando el mismo script en otro dispositivo, se puede usar las configuraciones ya preestablecidas de manera mas dinámica.

El script se encarga de clonar las carpetas necesarios para mantener las configuraciones del sistema, sin embargo, 
las carpetas clonadas deben ser subiadas a un servicio en la nube para que el script pueda usarlas luego.
Las carpetas que se clonan pueden ser cambiadas desde el código.
